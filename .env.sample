# Docker Compose のリソース名を学生ごとに分離するため、必ず一意の値に変更してください。
# 使用できる文字は小文字、数字、ハイフン、アンダースコアです。
COMPOSE_PROJECT_NAME=studentXX-webapp # ここのstudentXX の XX を指定された数値に変更してください

# VPSのホスト側で公開するHTTPポートです。他の学生と重複しない値を指定してください。
# コンテナ内でnginxが使用する80番ポートは変更されません。
WEB_PORT=80XX # ここを指定されたポート番号に変更してください
